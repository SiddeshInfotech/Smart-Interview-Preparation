FROM ghcr.io/engineer-man/piston:latest

# Environment variables required for cloud sandboxing
ENV PORT=2000
ENV DISABLE_SECURITY=true

# Pre-install compilers and interpreters during image build using Node CLI
RUN node /piston/repo/cli/index.js install python gcc java node

EXPOSE 2000
